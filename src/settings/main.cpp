// Placeholder entry point. The real GTK4 settings/power-menu UI lands in
// Phase 6 of the roadmap; this stub only exists so the build produces a
// `fleetwm-settings` binary while Phase 0 validates the compositor.
int main() {
  return 0;
}
