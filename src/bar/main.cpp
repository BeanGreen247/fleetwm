// Placeholder entry point. The real GTK4 + gtk4-layer-shell bar UI
// (workspace switcher, clock, stats, power icon) lands in Phase 3 of the
// roadmap; this stub only exists so the build produces a `fleetwm-bar`
// binary while Phase 0 validates the compositor.
int main() {
  return 0;
}
